{#
/**
 * @file
 * Theme override for a development objective's number field.
 * @see template_preprocess_field()
 */
#}

{% for item in items %}
  <div{{ item.attributes.addClass('number--development-objective') }}>{{ item.content }}</div>
{% endfor %}
